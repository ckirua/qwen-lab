# autonomous-hello Campaign Plan

## Goal
Create a simple C++ project to demonstrate basic CMake and testing setup.

## File List
- `sandbox/hello-cpp/CMakeLists.txt`
- `sandbox/hello-cpp/main.cpp`

## Test Command
Run `ctest` to verify the build and test setup.

## GO Criteria
ctest passes once under `sandbox/hello-cpp/build` and `plan.md` exists.